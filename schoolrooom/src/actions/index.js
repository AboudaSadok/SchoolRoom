import axios from 'axios';
import ROOT_URL from './consts';

export const FETCH_PERSON = "fetch_person";
/*export function fetchPerson() {
  //const request = axios.get(`${ROOT_URL}/person/${id}`);

  return {
    type: FETCH_PERSON,
    payload: request
  };
}
*/
